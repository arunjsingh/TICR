# Using git clone 
  clone https://github.com/arunjsingh/TICR

  git checkout master


# Execute the code
From the main folder within the code base/repo 
  make run    # This will install and run the backend portion of the TICR.

  npm run dev # This will execute the UI.